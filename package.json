{
  "name": "aedex",
  "private": true,
  "version": "0.0.0",
  "description": "Aedex CLI monorepo — the Aegntic Decentralized Exchange CLI. Discover, inspect, and run hundreds of data tools. One CLI, one balance, every data tool.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.4",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "@types/node": "^22.10.0"
  }
}
